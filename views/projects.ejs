<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniPlan ● projects</title>
</head>

<body>
    <%- include('../public/partials/navbar'); %>

    <h1 class="m-4">All Projects</h1>

    <div class="container">
        <div class="wrapper">
            <% projects.forEach(project => { %>
            <div class="card">
                <h2><%= project.project_name %></h2>
                <p><%= project.project_bio %></p>
                <a href="/project/<%= project.project_id %>">View Project</a>
            </div>
            <% }); %>
        </div>
    </div>
</body>

</html>